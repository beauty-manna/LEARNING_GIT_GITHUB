# LEARNING_GIT_GITHUB
I am learning git and github from apna college, youtube. 
<br>
Author: Beauty Manna
<br>
Docs: .md , md means markdown. "cd" means change directory. 'ls' for show all files in the folder. 'ls -Force or ls -Hidden' for show all hidden files.
<br>
'git clone HTTP link of repo' for clone(global repo content to local repo). 'git status' for check the status. 'git add .' for add all the changes. 'git commit -m "add changes"' for commite. 'git push origin main' for push local repo content to global repo.
